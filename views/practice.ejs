<html>
    <head>
       <title>
           <%= title %>
       </title>
    </head>

    <body>
        <h1>
            Playground
        </h1>

        <ul>
            <% for(let i =1; i<11; i++){ %>
            <li>Sumit Have <%= i %> Chocalate</li>
            <% } %>
        </ul>

        <% if(1){ %>
            <h1>It's True!</h1>
        <% } else { %>
            <p>It's False</p>
        <% } %>
    </body>
</html>